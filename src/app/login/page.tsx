'use client';

export default function LoginPage() {
  return (
    <div style={{ maxWidth: 400, margin: 'auto', padding: 32 }}>
      <h2>Login</h2>
      <p>Login via Firebase foi removido deste projeto.</p>
    </div>
  );
}